# Local development (Render uses dashboard env vars — see phase8_backend_deploy/.env.render.example)

DATA_PATH=
CORS_ORIGINS=http://localhost:3000
API_HOST=127.0.0.1
API_PORT=8000

# OpenAI (or copy from phase4_recommendation_engine/.env)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Set API_RELOAD=false to run without auto-reload locally
# API_RELOAD=true
